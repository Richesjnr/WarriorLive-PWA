{
  "collections": [
    {
      "name": "users",
      "documentId": "uid",
      "description": "User profiles.",
      "schema": {
        "email": "string",
        "displayName": "string",
        "createdAt": "timestamp"
      },
      "rules": "allow read, write: if request.auth != null && request.auth.uid == document;",
      "indexes": []
    }
  ]
}
